# 프로덕션 환경변수 (Railway용)
NODE_ENV=production
PORT=3000

# 데이터베이스 설정 (Gabia MySQL)
DB_HOST=my8003.gabiadb.com
DB_PORT=3306
DB_USER=your_production_username
DB_PASSWORD=your_secure_production_password
DB_NAME=your_production_database
DB_CHARSET=utf8mb4
DB_TIMEZONE=+09:00

# 세션 보안 (반드시 강력한 랜덤 키 사용)
SESSION_SECRET=generate_secure_random_string_32_characters_long

# CORS 설정 (Vercel 배포 후 실제 도메인으로 변경)
FRONTEND_URL=https://your-app.vercel.app

# 선택사항: 로깅 레벨
LOG_LEVEL=info